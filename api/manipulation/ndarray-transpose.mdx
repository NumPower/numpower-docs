# NDArray::transpose

```php
public static function transpose(NDArray|array|scalar $a): NDArray;
```
Return the transpose of matrix `$a`

---

## Notes

:::tip

#### GPU SUPPORTED
This operation is supported by GPU (VRAM).

:::