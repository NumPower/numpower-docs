import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# NDArray::isGPU

```php
public function isGPU(): bool;
```
Check if the NDArray is stored in the GPU.

---

## Return

> **Type** `bool`
>- `True` if NDArray is stored in the GPU, `False` otherwise.
