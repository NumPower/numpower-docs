import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# NDArray::clip

```php
public static function clip($a, $min, $max);
```

## Notes

:::tip

#### GPU SUPPORTED
This operation is supported by GPU (VRAM) and contains a custom CUDA kernel.

:::
