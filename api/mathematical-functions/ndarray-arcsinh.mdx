import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# NDArray::arcsinh

```php
public static function arcsinh($a);
```

## Notes

:::tip

#### GPU SUPPORTED
This operation is supported by GPU (VRAM) and contains a custom CUDA kernel.

:::
