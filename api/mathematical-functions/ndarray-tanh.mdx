import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# NDArray::tanh

```php
public static function tanh($a);
```

## Notes

:::tip

#### GPU SUPPORTED
This operation is supported by GPU (VRAM) and contains a custom CUDA kernel.

:::
