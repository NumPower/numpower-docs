import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# NDArray::sinh

```php
public static function sinh($a);
```

## Notes

:::tip

#### GPU SUPPORTED
This operation is supported by GPU (VRAM) and contains a custom CUDA kernel.

:::
