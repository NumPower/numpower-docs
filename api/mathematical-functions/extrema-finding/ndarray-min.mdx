# NDArray::min

```php
public static function min(NDArray|array|scalar $a): double;
```
Finds the minimum value in the NDArray.

---

## Notes

:::tip

#### GPU SUPPORTED
This operation is supported by GPU (VRAM) and contains a custom CUDA kernel.

:::