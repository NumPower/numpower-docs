# NDArray::max

```php
public static function max(NDArray|array|scalar $a): double;
```
Finds the maximum value in the NDArray.

---

## Notes

:::tip

#### GPU SUPPORTED
This operation is supported by GPU (VRAM) and contains a custom CUDA kernel.

:::
