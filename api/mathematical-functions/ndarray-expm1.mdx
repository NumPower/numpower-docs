import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# NDArray::expm1

```php
public static function expm1($a);
```

## Notes

:::tip

#### GPU SUPPORTED
This operation is supported by GPU (VRAM) and contains a custom CUDA kernel.

:::
