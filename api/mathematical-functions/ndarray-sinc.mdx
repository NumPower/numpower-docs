import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# NDArray::sinc

```php
public static function sinc($a);
```

## Notes

:::tip

#### GPU SUPPORTED
This operation is supported by GPU (VRAM) and contains a custom CUDA kernel.

:::
