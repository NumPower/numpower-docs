import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# NDArray::arccosh

```php
public static function arccosh($a);
```

## Notes

:::tip

#### GPU SUPPORTED
This operation is supported by GPU (VRAM) and contains a custom CUDA kernel.

:::
