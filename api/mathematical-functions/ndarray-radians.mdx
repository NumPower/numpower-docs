import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# NDArray::radians

```php
public static function radians($a);
```

## Notes

:::tip

#### GPU SUPPORTED
This operation is supported by GPU (VRAM) and contains a custom CUDA kernel.

:::
