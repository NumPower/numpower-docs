import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# NDArray::degrees

```php
public static function degrees($a);
```

## Notes

:::tip

#### GPU SUPPORTED
This operation is supported by GPU (VRAM) and contains a custom CUDA kernel.

:::
